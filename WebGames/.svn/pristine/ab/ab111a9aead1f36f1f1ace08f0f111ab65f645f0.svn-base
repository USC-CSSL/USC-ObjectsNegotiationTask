/*
package edu.usc.cct.rapport.web_games.client;

import java.util.HashMap;
import java.util.Set;

import edu.usc.cct.rapport.web_games.client.support.tuple.Pair;


public class LocalizableMatrix {

	final HashMap<Pair<String, String>, String> localizedMatrix;

	public LocalizableMatrix(final Set<String> set1, final Set<String> set2) {

		this.localizedMatrix = new HashMap<Pair<String,String>, String>();

		for (final String a: set1) {
			for (final String b: set2) {
				final Pair<String, String> key = new Pair<String, String>(a, b);
				
			};
		};
		
	};

/*
	final private EnumMap<B, EnumMap<L, String>> localizedEnumMatrix;

	public LocalizableEnumMatrix(final B base, final L localizable) {
		final Class<? extends Enum> classB = base.getClass();
		classB.values();
		final Class<L> classL = localizable.getDeclaringClass();
		
		this.localizedEnumMatrix = new EnumMap<B, EnumMap<L, String>>(classB);
		for (final B baseValue: Enum<B>.values()) {
			
		};
	};

/*
	public LocalizableEnumMatrix(final B base, final L localizable) {
		final Class<B> classB = base.getDeclaringClass();
		final Class<L> classL = localizable.getDeclaringClass();
		
		this.localizedEnumMatrix = new EnumMap<B, EnumMap<L, String>>(classB);
		for (final B baseValue: Enum<B>.values()) {
			//
		};
	};
};
*/

package edu.usc.cct.rapport.web_games.client.instructions;

import com.google.gwt.event.shared.EventBus;
import com.google.inject.Inject;
import com.google.inject.assistedinject.Assisted;

import edu.usc.cct.rapport.web_games.client.WebGamesPlace;

public class InstructionsPlace extends WebGamesPlace implements IInstructionsPlace {

	static final String placeName = "instructions";

	@Inject
	public InstructionsPlace(final EventBus eventBus, @Assisted final String token) {
		super(eventBus, placeName, token, false);

	};


/*
	@Override
	public WebGamesActivity getActivity(final WebGamesGinjector webGamesGinjector) {
		webGamesGinjector.setInstructionsPlace(this);
	    final WebGamesActivity result = webGamesGinjector.getInstructionsActivity();
		return result;
	};
*/
};
